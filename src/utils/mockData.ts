import { School, Post, User } from '../types';

// All records removed as requested - the website starts completely empty for production posting
export const INITIAL_SCHOOLS: School[] = [];
export const DEMO_USERS: Record<string, User> = {};
export const INITIAL_POSTS: Post[] = [];
